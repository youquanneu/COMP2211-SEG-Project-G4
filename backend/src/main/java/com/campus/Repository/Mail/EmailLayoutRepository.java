package com.campus.Repository.Mail;

import com.campus.Entity.Mail.EmailLayout;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EmailLayoutRepository extends JpaRepository<EmailLayout,Integer> {
}
